
Read `AGENTS.md` before executing this file.

We're adding the design system and UI primitive components

Install and configure `shadcn/ui`

Add these shadcn components:
- Button
- Card
- Dialog
- Input
- Calendar
- Avatar
- Toast
- Spinner
- Skeleton

Do not modify the generated `components/ui/*` after the installation.

Also install `lucide-react`.

Ensure all of the components match the theme in `global.css`


### check when done

- All components import without errors
- No light default styling appears